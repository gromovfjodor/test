(function(){let container=document.querySelector('.rating');let items=container.querySelectorAll('.rating-item')container.onclick=function(e){if(sessionStorage.getItem('check')!=null){alert('Вы уже голосовали в данном опросе!');return!1};sessionStorage.setItem('check',1);if(!e.target.classList.contains('active')){items.forEach(function(item){item.classList.remove('active')});e.target.classList.add('active')}}})();function move(){let elem=document.getElementById("Bar");let value=document.getElementById("select").value;function frame(){elem.style.width=value+'%';elem.innerHTML=value*1+'%'}frame()}function send(){let text='Привет мир!';const request=new XMLHttpRequest();const url="../../assets/scripts/index.php";const params="message="+text;request.open("POST",url,!0);request.setRequestHeader("Content-type","application/x-www-form-urlencoded");request.addEventListener("readystatechange",()=>{if(request.readyState===4&&request.status===200){let text=document.getElementById('message');text.innerHTML=request.responseText}});request.send(params)}